<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function feeds_tamper_markup_manip_ctools_plugin_directory($owner, $plugin_type){
  if ($owner == 'feeds_tamper' && $plugin_type == 'plugins') {
    return 'plugins';
  }
}
